<%= form_for @user_image, :html => { :multipart => true } do |f| %>
  <% if @user_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_image.errors.count, "error") %> prohibited this user_image from being saved:</h2>

      <ul>
      <% @user_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :caption %><br />
    <%= f.text_area :caption %>
  </div>
  <div class="field">
    <%= f.label :file %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
